const gulp = require('gulp');
const branch = require('./index');

gulp.task('branch', (done) => {
  branch('git@bitbucket.org:ydp/sslideshow-simulation-engine-dist.git', '1.0.8').then(function() {
    done();
  }, function(err) {
    console.log(err);
  });
});
